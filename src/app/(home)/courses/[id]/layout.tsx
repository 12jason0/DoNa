"use client";

import React from "react";

/** 코스 상세 세그먼트 공통 레이아웃 — 실제 UI는 `page.tsx` + `CourseDetailClient` */
export default function CoursesIdLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
